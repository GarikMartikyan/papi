import { PAPI_ROUTES } from '@papi/routing';

/**
 * Адреса панели — вместе с теми, что заводит ядро.
 *
 * Список один на всю панель: и маршруты, и ссылки, и редиректы берут адрес
 * отсюда, поэтому переименование раздела правится в одном месте.
 *
 * `PAPI_ROUTES` разворачивается сюда же, а не импортируется отдельно: вход и
 * корень — такие же адреса панели, просто заведённые ядром, и разносить их по
 * двум спискам значит помнить, в каком из них искать.
 */
export const ROUTES = {
  ...PAPI_ROUTES,
  users: '/users',
  settings: '/settings',
} as const;
