/// <reference types="vite/client" />

/**
 * Переменные окружения панели.
 *
 * Переменные ядра лежат в `lib/vite-env.d.ts` — интерфейс тот же, TypeScript
 * сливает объявления, поэтому дублировать их здесь не нужно.
 *
 * Каждая новая `VITE_*` панели дописывается сюда — иначе она снова станет `any`
 * из-за индексной сигнатуры в `vite/client`. Значение по умолчанию идёт
 * в `.env.example`.
 *
 * TODO: заглушка — пустой интерфейс. Своих переменных у скелета нет; панель
 * дописывает первую и убирает этот комментарий.
 */
interface ImportMetaEnv {}
