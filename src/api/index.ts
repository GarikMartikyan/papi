/**
 * API панели — её эндпоинты поверх api ядра.
 *
 * Своего api панель не создаёт: он один, живёт в ядре и приходит из
 * `@papi/api`. Файлы в `endpoints/` дописывают в него свои ручки, а теги, что
 * им нужны, объявлены в `tags.constants` — как это выглядит, видно в
 * `endpoints/users.api.ts`.
 *
 * Ресурс — файл: новая сущность заводит свой `<имя>.api.ts` рядом и строку в
 * этот барель.
 *
 * Адрес бэкенда сюда не передаётся — ядро читает его из `VITE_API_BASE_URL`.
 */

export * from './endpoints/users.api';
export * from './tags.constants';
