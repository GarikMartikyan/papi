{
  "$schema": "https://json.schemastore.org/tsconfig",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",

    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,

    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,

    "noEmit": true,

    /*
     * Ядро видно панели только под этим именем. Записей две, потому что
     * `@papi/*` не покрывает голый `@papi` — у шаблона обязателен хвост.
     *
     * Тот же алиас продублирован в `vite.config.ts`: TypeScript отвечает за
     * типы, Vite — за резолв на рантайме, и знать друг о друге они не умеют.
     */
    "baseUrl": ".",
    "paths": {
      "@papi": ["lib/index.ts"],
      "@papi/*": ["lib/*"]
    },

    /*
     * `vite/client` объявляет модули для ассетов (`*.svg`, `*.css`) и сам
     * `import.meta.env`, поэтому общего `global.d.ts` у ядра нет.
     *
     * Отдельные переменные окружения всё же описаны — ядро в `lib/vite-env.d.ts`,
     * панель в `src/vite-env.d.ts`: `ImportMetaEnv` у `vite/client` идёт с
     * индексной сигнатурой `any`, и без объявления чтение переменной было бы
     * нетипизированным.
     */
    "types": ["vite/client", "node"]
  },
  "include": ["lib", "src", "vite.config.ts", ".claude/skills/*/example"],
  "exclude": ["dist", "node_modules"]
}
