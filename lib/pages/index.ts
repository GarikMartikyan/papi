/**
 * Pages — экраны, которые ядро возит с собой: вход и ненайденный адрес.
 *
 * Обе страницы `PapiRouter` ставит сам, поэтому панели импортировать их обычно
 * не нужно. Барель существует ради второго случая: панель показывает вход в
 * своём обрамлении или хочет отрисовать 404 внутри собственного маршрута.
 */

export * from './LoginPage/LoginPage';
export * from './NotFoundPage/NotFoundPage';
