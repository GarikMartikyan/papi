/**
 * Routing — маршруты панели, гард и каркас вокруг них.
 *
 * `RouteProtector` и `PapiRouterLayout` наружу не выходят: это внутренности
 * `PapiRouter` — гард и каркас, два маршрута-обёртки, сами по себе
 * бессмысленные. Панель получает их вместе с `PapiRouter`, другого способа
 * собрать маршруты панели у неё и нет.
 *
 * `PermissionGate` выходит: `PapiRouter` заворачивает им страницу раздела, но
 * тем же компонентом панель закрывает правом что угодно у себя — кнопку,
 * колонку, секцию.
 */

export * from './PapiRouter';
export * from './PermissionGate';
