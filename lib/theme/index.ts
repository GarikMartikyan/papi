/**
 * Theme — готовые antd-темы ядра.
 *
 * Панель их не подключает руками: базовую ставит `ThemeProvider`, а своё панель
 * передаёт ему пропом `theme` — оно ложится сверху.
 */

export * from './base.theme';
