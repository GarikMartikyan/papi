/**
 * i18n — переводы строк самого ядра.
 *
 * Каталоги, а не механика: активный язык, список языков и подключение
 * react-intl живут в `I18nProvider` и `store/slices/config.slice`. Здесь только
 * то, что ядро умеет сказать на своей стороне.
 *
 * Панели этот барель обычно не нужен — провайдер подмешивает каталоги сам, ещё
 * до того, как она о них узнает. Наружу они выходят для панели, которая
 * собирает `IntlProvider` мимо `PapiProvider` или хочет посмотреть, какие
 * строки перекрывает.
 */

export * from './messages';
