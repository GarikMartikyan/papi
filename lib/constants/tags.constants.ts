/**
 * Теги RTK Query, которые заводит ядро.
 *
 * Отдельным файлом, а не строками по месту: тег — это связь между двумя точками
 * (`providesTags` в одной, `invalidatesTags` в другой), и опечатка в такой
 * строке ничего не ломает на сборке. Она просто тихо перестаёт инвалидировать
 * кеш, и ищут потом не опечатку, а «почему список не обновился».
 *
 * `as const` тут не украшение: набор тегов строится по литеральным типам, и без
 * него значения станут обычным `string` — проверка исчезнет, и в `providesTags`
 * пройдёт что угодно.
 *
 * Отсюда они целиком уходят в `tagTypes` при `createApi`, поэтому файлам
 * эндпоинтов ядра ничего добавлять не нужно — они сразу зовут `injectEndpoints`.
 *
 * Теги панели живут у неё же — ядро о её сущностях не знает. Она объявляет их
 * одним `injectTags` (см. `src/constants/tags.constants.ts`) и получает оттуда
 * весь набор целиком, вместе с этими, — импортировать их отсюда ей не нужно.
 */
export const papiRtkTags = {
  me: 'Me',
} as const;
