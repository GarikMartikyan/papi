/**
 * Теги RTK Query, которые заводит ядро.
 *
 * Отдельным файлом, а не строками по месту: тег — это связь между двумя точками
 * (`providesTags` в одной, `invalidatesTags` в другой), и опечатка в такой
 * строке ничего не ломает на сборке. Она просто тихо перестаёт инвалидировать
 * кеш, и ищут потом не опечатку, а «почему список не обновился».
 *
 * `as const` тут не украшение: `enhanceEndpoints` работает по литеральным типам,
 * и без него значения станут обычным `string` — набор тегов перестанет
 * проверяться, и в `providesTags` пройдёт что угодно.
 *
 * Теги панели живут у неё же — ядро о её сущностях не знает. Понадобилось
 * инвалидировать тег ядра из панели: он перечисляется в её `addTagTypes` рядом
 * со своими, потому что `createApi` фиксирует набор при создании и каждый файл
 * эндпоинтов объявляет всё, чем пользуется.
 */
export const rtkTags = {
  me: 'Me',
} as const;
